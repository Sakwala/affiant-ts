/**
 * `@affiant/core/testing` — the fixture runner and the helpers a host or a
 * conformance driver reuses to exercise a gate.
 *
 * The entry point exists from the first release so a host can pin the import path
 * and a conformance driver can name it. The runner itself — the declarative fixture
 * shape `{ id, rules, given, expect }`, the stub ports and the assertion helpers —
 * arrives in pull request **C7 (`core/sequences`)**, alongside the seeded fixtures
 * for the two v0.1 sequences.
 *
 * @packageDocumentation
 */

export {};
